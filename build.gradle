plugins {
    id 'nva.download.file.rootplugin'
}

//workaround for jacoco-merge to work
allprojects {
    apply plugin: 'nva.download.file.java-conventions'
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = '8.2.1'
}